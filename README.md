# Artificial-Intelligence-And-Open-Science-In-Research-Software-Engineering
This repository performs a text analysis over 10 open-access research papers
using Grobid to extract structured information from PDF documents.

We selected 10 open-access papers from arXiv related to distributed
computing and data-intensive systems.

1. PDFs are processed using Grobid.
2. Grobid generates TEI XML files.
3. XML files are analyzed to extract:
   - Abstract text
   - Figures
   - Links

## Requirements
Python libraries:
- beautifulsoup4
- matplotlib
- wordcloud
- requests

Grobid image in docker: https://hub.docker.com/r/grobid/grobid/tags



## Reproducibility

1. Run Grobid with Docker:
docker run -t --rm -p 8070:8070 grobid/grobid:${latest_grobid_version}-crf


2. Process the PDFs:
python scripts/run_grobid.py

3. Generate the analysis:

python grobidClient
python wordCloud.py
python countFigures.py
python extract_links.py



## Validation
### Grobid Extraction

We validated that Grobid correctly parsed the PDF documents by
inspecting the generated TEI XML files.

We verified that the following sections were correctly extracted:
- abstract
- figures
- references
- links

These elements were consistent with the structure of the original PDF
documents.


### Word Cloud

To validate the word cloud generation, we manually inspected several XML files
generated by Grobid and compared the extracted abstract text with the
original PDF documents.

We verified that:
- The abstract section was correctly extracted from the `<abstract>` tag.
- The most frequent words appearing in the word cloud (e.g., "data",
  "performance", "computation") are consistent with the topics discussed
  in the abstracts.

This confirms that the word cloud correctly represents the dominant
terms present in the dataset.

### Figure Analysis

The number of figures was obtained by counting the `<figure>` tags
in the TEI XML files generated by Grobid.

To validate the results, we manually inspected several PDF papers
and counted the figures appearing in the document.

We compared this manual count with the number of `<figure>` elements
found in the XML files and the output produced by our script.
The results matched for the inspected samples, confirming that the
figure extraction process is correct.

### Validation of Link Extraction

Links were extracted from the TEI XML files by searching for `<ref>`
elements containing a `target` attribute.

To validate the extraction process, we manually inspected several
XML files and identified the links extracted by our script.
We then compared these links with those appearing in the original
PDF documents.

The extracted links correspond to URLs present in the papers,
confirming that the extraction process works as expected.

## Results

### Word Cloud from Abstracts

![Word Cloud](results/wordcloud.png)

The word cloud represents the most frequent terms appearing in the
abstracts of the analyzed papers.

### Number of Figures per Article

![Figures per paper](results/figures_per_paper.png)

This visualization shows the number of figures detected in each
article based on the `<figure>` elements extracted from the XML files.

## License
MIT License